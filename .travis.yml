language: php
php:
  - 5.5
  - 5.6
  - 7.0
install:
  - composer install
script:
  - mkdir -p build/logs
  - phpunit --coverage-clover build/logs/clover.xml
after_script:
  - php vendor/satooshi/php-coveralls/bin/coveralls -v
